import random
import time

def generate_screenshot_demo(number):
    '''
    Generate a string with an extension jpg
    '''
    # Random number, the timestamp and the extension with a img identification
    return str(number)+"_"+"_img.png"